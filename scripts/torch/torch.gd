extends Node


# TODO: activate torch when player touches it with an already light up torch

# TODO: define lights and associate it with tile with blown off torches; on activate --> animate torches

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
